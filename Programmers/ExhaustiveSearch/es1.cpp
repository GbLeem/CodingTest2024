#include <string>
#include <vector>
#include <algorithm>

using namespace std;
int w = max(sizes[0][0], sizes[0][1]);
int h = min(sizes[0][0], sizes[0][1]);

pair<int, int> fit(int v1, int v2)
{

}

int solution(vector<vector<int>> sizes)
{
    int answer = 0;



    return answer;
}