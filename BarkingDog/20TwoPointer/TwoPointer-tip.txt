1. 배열을 만들고 그 배열에 st, en이라는 포인터 두기

2. 배열의 맨 처음 값에 대한 처리를 하고 시작하는 것이 좋음

3. 바깥 for문에 st값 움직이기
   안쪽 while문에서 en값에 대한 조건 밑 문제에서 주어지는 조건 처리
	-조건 예시) 배열에 연속된 홀수, 배열의 합이 얼마 이하, 이미 배열에 있는 수인지 등

4. 안쪽 while문에서 en 값 증가시키고, 조건에 대한 처리를 수행

5. while문 밖에서 값이 될 변수값 설정 
	- 예시) ans += (en-st+1);  ans = max(ans, en-st+1); 
   및 st가 가리키는 곳에 대한 값 처리
	- 예시) s[a[st]] = 0;